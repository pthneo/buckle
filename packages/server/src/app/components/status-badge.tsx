// import type { ServiceStatus } from "@/app/types/services";

// const statusConfig: Record<ServiceStatus, { color: string; label: string }> = {
//   online: { color: "text-chart-2", label: "ONLINE" },
//   offline: { color: "text-destructive", label: "OFFLINE" },
//   unknown: { color: "text-muted-foreground", label: "UNKNOWN" },
// };

// export function StatusBadge({ status }: { status: ServiceStatus }) {
//   const config = statusConfig[status];
//   return (
//     <span className={`inline-flex items-center gap-1.5 font-medium text-xs ${config.color}`}>
//       <span className="inline-block h-1.5 w-1.5 rounded-full bg-current" />
//       {config.label}
//     </span>
//   );
// }

export function StatusBadge() {
  return null;
}
